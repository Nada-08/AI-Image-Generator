import { type AxiosInstance } from 'axios';
import type { PicogenAIOptions, RunOptions } from './ressources';
export declare class PicogenAI {
    client: AxiosInstance;
    constructor({ apiKey }: PicogenAIOptions);
    run(options: RunOptions): Promise<object>;
    cost(options: RunOptions): Promise<object>;
    get(id: string): Promise<object>;
    list(page: number, limit: number): Promise<object>;
}
export default PicogenAI;
